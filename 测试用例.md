# AICoach 全流程手动测试用例

## 自动化测试结果汇总

| # | 测试项 | 结果 | 备注 |
|---|--------|------|------|
| 1 | 注册 + 登录 | ✅ 通过 | |
| 2 | Dashboard 空状态 + 发送跳转 | ✅ 通过 | |
| 3 | ChatPlanner 对话 + LLM 回复 | ✅ 通过 | |
| 4 | 生成计划 + 跳转 Dashboard | ✅ 通过 | |
| 5 | 计划冲突检测 | ✅ 通过 | 修复后通过 |
| 6 | 会话列表切换 | ✅ 通过 | 修复后通过 |

### 自动化测试中发现并修复的 Bug

| Bug # | 描述 | 修复方式 |
|-------|------|----------|
| 6 | `NameError: name 'TaskStatus' is not defined` — 发送消息时后端 500 | `chat.py` 添加 `from app.db.models.task import Task, TaskStatus` |
| 7 | 冲突检测查询 `scheduled_date == today` 匹配不到日计划任务（scheduled_date 为 NULL） | 改为 `or_(Task.scheduled_date == today, Task.scheduled_date.is_(None))` |
| 8 | 切换会话时上一个会话的冲突警告残留 | `handleSelectSession` 中添加 `setSessionState(null)` |

---

## 手动测试用例

### 一、用户认证模块

#### TC-01 注册新用户
- **前置条件**: 无
- **步骤**:
  1. 打开应用，进入注册页面
  2. 输入用户名（如 `manual_test`）和密码（如 `test123456`）
  3. 点击"注册"按钮
- **预期结果**:
  - 注册成功提示
  - 自动跳转到登录页面或自动登录

#### TC-02 登录
- **前置条件**: 已有注册账号
- **步骤**:
  1. 在登录页面输入用户名和密码
  2. 点击"登录"
- **预期结果**:
  - 登录成功，跳转到 Dashboard
  - 顶部显示用户名

#### TC-03 登录失败
- **前置条件**: 无
- **步骤**:
  1. 输入错误密码
  2. 点击"登录"
- **预期结果**:
  - 显示"用户名或密码错误"提示
  - 不跳转

#### TC-04 JWT Token 过期处理
- **前置条件**: 已登录
- **步骤**:
  1. 等待 Token 过期（或清除 localStorage 中的 token）
  2. 刷新页面或执行任何 API 操作
- **预期结果**:
  - 自动跳转到登录页面
  - 不显示白屏或报错

---

### 二、Dashboard 模块

#### TC-05 空状态展示
- **前置条件**: 新用户，无任何计划
- **步骤**:
  1. 登录后查看 Dashboard
- **预期结果**:
  - 显示空状态提示（引导用户输入目标）
  - 输入框可用，发送按钮禁用状态

#### TC-06 从 Dashboard 发送消息跳转到 ChatPlanner
- **前置条件**: 已登录
- **步骤**:
  1. 在 Dashboard 顶部输入框输入目标（如"今天学1小时Python"）
  2. 点击"发送"按钮
- **预期结果**:
  - 跳转到 ChatPlanner 页面
  - 自动创建新会话
  - 用户输入的消息已显示在聊天记录中
  - LLM 自动回复

#### TC-07 导航栏按钮顺序
- **前置条件**: 已登录，在 Dashboard
- **步骤**:
  1. 查看顶部导航栏按钮从左到右的顺序
- **预期结果**:
  - 顺序为：切换主题 → AI 对话 → 历史 → 复盘 → 个性设置 → 退出

#### TC-08 AI 对话按钮跳转（无消息）
- **前置条件**: 已登录，Dashboard 有/无任务
- **步骤**:
  1. 不在输入框输入任何内容
  2. 直接点击"AI 对话"按钮
- **预期结果**:
  - 跳转到 ChatPlanner 页面
  - 不崩溃，不报错
  - 显示空对话引导界面

---

### 三、ChatPlanner 对话模块

#### TC-09 新建对话
- **前置条件**: 在 ChatPlanner 页面
- **步骤**:
  1. 点击左侧"新建对话"按钮
- **预期结果**:
  - 左侧列表顶部出现新的"新对话"项
  - 右侧显示空对话引导界面
  - 输入框变为可用状态
  - 不创建多个重复会话（StrictMode 保护）

#### TC-10 发送消息 + LLM 回复
- **前置条件**: 已创建对话
- **步骤**:
  1. 在输入框输入消息（如"我想学游泳"）
  2. 按 Enter 或点击发送按钮
- **预期结果**:
  - 用户消息立即显示（蓝色气泡，右对齐）
  - 显示加载动画（三个跳动的点）
  - LLM 回复显示（白色气泡，左对齐，带 sparkle 图标）
  - 回复内容与用户输入相关

#### TC-11 多轮对话
- **前置条件**: 已有一轮对话
- **步骤**:
  1. 继续在输入框输入回复（如"今天下午去"）
  2. 发送
- **预期结果**:
  - 新消息追加到聊天记录
  - LLM 根据上下文继续对话
  - 会话标题自动更新为首条用户消息

#### TC-12 生成计划按钮出现
- **前置条件**: LLM 判断信息充足
- **步骤**:
  1. 进行多轮对话，提供足够的目标和时间信息
  2. 等待 LLM 回复
- **预期结果**:
  - 当 `ready_to_finalize=true` 时，消息下方出现"✨ 生成计划"按钮
  - 按钮有渐变背景和阴影效果

#### TC-13 生成计划 + 跳转 Dashboard
- **前置条件**: "✨ 生成计划"按钮已出现
- **步骤**:
  1. 点击"✨ 生成计划"按钮
- **预期结果**:
  - 显示"正在生成计划..."加载状态
  - 生成完成后自动跳转到 Dashboard
  - Dashboard 显示新生成的任务卡片
  - 任务内容与对话中讨论的目标一致

#### TC-14 上下文感知生成
- **前置条件**: 对话中提到了具体安排
- **步骤**:
  1. 在对话中明确说"今晚先看视频学习"
  2. 等待 LLM 确认后点击生成计划
- **预期结果**:
  - 生成的任务包含"看视频"相关内容
  - 不会安排不可能的时间段（如深夜去现场学习）

---

### 四、计划冲突检测模块

#### TC-15 有未完成任务时的新对话冲突警告
- **前置条件**: Dashboard 有未完成的待办任务
- **步骤**:
  1. 点击"AI 对话"进入 ChatPlanner
  2. 新建对话
  3. 发送任意消息
- **预期结果**:
  - LLM 回复后，消息下方出现**琥珀色警告横幅**
  - 警告内容包含：未完成任务数量、来源计划名称
  - 警告提示继续生成将替换当前计划

#### TC-16 无未完成任务时无冲突警告
- **前置条件**: 所有任务已完成或无任务
- **步骤**:
  1. 进入 ChatPlanner
  2. 新建对话并发送消息
- **预期结果**:
  - 不显示冲突警告横幅
  - LLM 正常回复

#### TC-17 冲突警告不影响对话继续
- **前置条件**: 冲突警告已显示
- **步骤**:
  1. 在有冲突警告的情况下继续输入消息
  2. 发送
- **预期结果**:
  - 可以正常发送和接收消息
  - 对话不阻塞

---

### 五、会话列表模块

#### TC-18 会话列表展示
- **前置条件**: 有多个历史会话
- **步骤**:
  1. 进入 ChatPlanner 页面
  2. 查看左侧会话列表
- **预期结果**:
  - 所有历史会话按创建时间倒序排列
  - 每个会话显示标题和状态标签（待定/日计划/长期）
  - 已定稿的会话显示 ✓ 标记

#### TC-19 切换会话加载历史消息
- **前置条件**: 有多个会话
- **步骤**:
  1. 点击左侧列表中的某个历史会话
- **预期结果**:
  - 该会话高亮选中
  - 右侧加载该会话的完整聊天记录
  - 上一个会话的状态（如冲突警告）被清除

#### TC-20 会话标题自动更新
- **前置条件**: 新建了空标题会话
- **步骤**:
  1. 在新会话中发送第一条消息
- **预期结果**:
  - 左侧会话标题自动更新为消息前30个字符
  - 不再显示"新对话"

---

### 六、任务管理模块（Dashboard）

#### TC-21 任务卡片展示
- **前置条件**: 已生成计划
- **步骤**:
  1. 在 Dashboard 查看任务卡片
- **预期结果**:
  - 任务按列分组：待办、进行中、已完成
  - 每个卡片显示任务标题、验收标准、时间段
  - 卡片有操作按钮：标记完成、重新生成、编辑

#### TC-22 标记任务完成
- **前置条件**: 有待办任务
- **步骤**:
  1. 点击任务卡片上的"标记完成"按钮
- **预期结果**:
  - 任务从"待办"列移到"已完成"列
  - UI 即时更新，无需刷新

#### TC-23 拖拽排序
- **前置条件**: 有多个待办任务
- **步骤**:
  1. 拖拽一个任务卡片到另一个位置
- **预期结果**:
  - 任务顺序更新
  - 拖拽过程流畅，无闪烁

#### TC-24 重新生成任务
- **前置条件**: 有待办任务
- **步骤**:
  1. 点击"重新生成"按钮
- **预期结果**:
  - 显示加载状态
  - 任务内容被 LLM 重新生成
  - 新内容替换旧内容

#### TC-25 编辑任务
- **前置条件**: 有待办任务
- **步骤**:
  1. 点击"编辑"按钮
  2. 修改任务标题
  3. 保存
- **预期结果**:
  - 任务标题更新
  - 修改后保持正确显示

---

### 七、加餐任务模块

#### TC-26 加餐任务按钮出现条件
- **前置条件**: 所有任务已完成，且任务数 >= 3
- **步骤**:
  1. 将所有任务标记为完成
  2. 查看 Dashboard
- **预期结果**:
  - 出现"加餐任务"提示或按钮
  - 提示用户可以生成额外任务

#### TC-27 生成加餐任务
- **前置条件**: "加餐任务"按钮已出现
- **步骤**:
  1. 点击"加餐任务"按钮
  2. 可选：填写复盘反馈
  3. 确认生成
- **预期结果**:
  - 生成 2-3 个额外任务
  - 任务追加到当前计划中
  - 任务时间在安静时段内

---

### 八、Quiet Hours（安静时段）模块

#### TC-28 安静时段偏好设置
- **前置条件**: 已登录
- **步骤**:
  1. 点击"个性设置"按钮
  2. 修改休息开始/结束时间
  3. 切换"允许夜间安排任务"开关
  4. 保存
- **预期结果**:
  - 设置保存成功
  - 后续生成的任务遵守安静时段

#### TC-29 安静时段确认弹窗
- **前置条件**: 安静时段偏好为不允许夜间安排
- **步骤**:
  1. 在 ChatPlanner 中讨论需要在夜间安排任务
  2. LLM 建议夜间任务
- **预期结果**:
  - 弹出确认弹窗："检测到你希望在夜间安排任务，是否更新偏好设置？"
  - 可选择"保持现状"或"允许夜间安排"

---

### 九、历史与复盘模块

#### TC-30 历史记录页面
- **前置条件**: 有历史计划
- **步骤**:
  1. 点击"历史"按钮
- **预期结果**:
  - 显示历史计划列表
  - 每条记录显示日期、目标、完成情况

#### TC-31 复盘页面
- **前置条件**: 有已完成或过期的计划
- **步骤**:
  1. 点击"复盘"按钮
- **预期结果**:
  - 显示复盘报告
  - 包含完成率、评价等信息

---

### 十、主题切换

#### TC-32 亮色/暗色主题切换
- **前置条件**: 无
- **步骤**:
  1. 点击"切换主题"按钮
  2. 再次点击
- **预期结果**:
  - 主题在亮色和暗色之间切换
  - 所有页面元素正确适配两种主题
  - 刷新后主题保持

---

### 十一、退出登录

#### TC-33 退出登录
- **前置条件**: 已登录
- **步骤**:
  1. 点击"退出"按钮
- **预期结果**:
  - 清除 Token
  - 跳转到登录页面
  - 无法通过后退按钮回到已登录页面

---

### 十二、边界情况与异常处理

#### TC-34 网络错误处理
- **前置条件**: 已登录
- **步骤**:
  1. 停止后端服务
  2. 在 ChatPlanner 中发送消息
- **预期结果**:
  - 显示"请求失败"错误提示
  - 不白屏，不崩溃
  - 有"关闭"按钮可关闭错误提示

#### TC-35 空消息发送
- **前置条件**: 已创建对话
- **步骤**:
  1. 不输入任何内容
  2. 按 Enter
- **预期结果**:
  - 不发送
  - 发送按钮保持禁用状态

#### TC-36 快速连续发送
- **前置条件**: 已创建对话
- **步骤**:
  1. 快速输入并发送多条消息
- **预期结果**:
  - 第一条发送时，输入框和发送按钮变为禁用
  - 不会出现重复发送或消息丢失

#### TC-37 长文本消息
- **前置条件**: 已创建对话
- **步骤**:
  1. 输入很长的消息（超过 500 字）
  2. 发送
- **预期结果**:
  - 消息正确显示，自动换行
  - 不溢出容器

#### TC-38 Shift+Enter 换行
- **前置条件**: 已创建对话
- **步骤**:
  1. 输入第一行
  2. 按 Shift+Enter
  3. 输入第二行
  4. 按 Enter 发送
- **预期结果**:
  - Shift+Enter 插入换行
  - Enter 发送整条消息（包含换行）

---

## 测试优先级建议

### P0（必须通过）
- TC-01 ~ TC-04: 用户认证
- TC-05 ~ TC-08: Dashboard 基础功能
- TC-09 ~ TC-13: ChatPlanner 核心流程
- TC-15: 冲突检测
- TC-21 ~ TC-22: 任务完成

### P1（重要功能）
- TC-14: 上下文感知生成
- TC-16 ~ TC-17: 冲突检测边界
- TC-18 ~ TC-20: 会话列表
- TC-23 ~ TC-25: 任务操作
- TC-28 ~ TC-29: 安静时段
- TC-32: 主题切换
- TC-33: 退出登录

### P2（完整体验）
- TC-26 ~ TC-27: 加餐任务
- TC-30 ~ TC-31: 历史与复盘
- TC-34 ~ TC-38: 边界情况
